export enum EFirebaseParams {
  LESS_THAN = '<',
  LESS_THAN_OR_EQUAL = '<=',
  EQUALS = '==',
  NOT_EQUALS = '!=',
  GREATER_THAN_OR_EQUAL = '>=',
  GREATER_THAN = '>',
  ARRAY_CONTAINS = 'array-contains',
  IN = 'in',
  ARRAY_CONTAINS_ANY = 'array-contains-any',
  NOT_IN = 'not-in',
}

export enum EFierbaseOrderBy {
  ASC = 'asc',
  DESC = 'desc',
}
